### Installation instructions for frontend
1. Install packages: ```npm install```
2. Serve development server: ```npm run dev```
3. Run linter: ```npm run lint```
4. Build project into ```static``` folder in top level folder: ```npm run build```

- NOTE: Refer to ```README.md``` at top level of project for starting the backend server